hooks:
 * replace all position variables (PROJECT, CODE_DIR, ...) by environ variables

remote:
 * display remote user in logs
 * additional options for rsync in the config file
 * lock system in the destination host to prevent multiple deploy (/var/lock/)

archives management
 * if the remote deploy failed, mark the archive dir ? (mv dir dir-failed)
 * what do we need to keep on the source machine ? only the last working version ?

new mode: manage archives

  deploy --list
list existant archives

  deploy -r /var/sig/foo_12345 example.com
remote deploy an exixtant archive 
